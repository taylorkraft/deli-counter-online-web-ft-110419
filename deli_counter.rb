def line(deli)
  if deli.length == 0 
    puts "The line is currently empty."
  else
    puts "The line in currently deli.each_with_index { |index, name| p "#{index}. #{name}" "
  end
end

deli.each_with_index { |index, name| p "#{index}. #{name}"